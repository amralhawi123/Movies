import {ALLMOVIES} from '../types'
export const moviesReducer = (state, action) => {
   switch (action.type) {
      case 'ALLMOVIES':
         
         break;
      default:
         break;
   }


}